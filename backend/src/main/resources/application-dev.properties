# Dev profile: disable mail health indicator so /actuator/health doesn't return 503
# when SMTP is not configured in local development.
management.health.mail.enabled=false

# Show full health details for easier debugging in dev
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=health,info
